<?xml version="1.0" encoding="utf-8"?>
<!--
  Debug builds only: lets the app reach a local Supabase stack over plain HTTP
  (10.0.2.2 is the host machine as seen from the emulator). Release builds keep
  Android's default, which refuses cleartext everywhere.
-->
<network-security-config>
    <domain-config cleartextTrafficPermitted="true">
        <domain includeSubdomains="false">10.0.2.2</domain>
        <domain includeSubdomains="false">localhost</domain>
    </domain-config>
</network-security-config>
