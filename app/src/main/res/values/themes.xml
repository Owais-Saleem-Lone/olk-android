<?xml version="1.0" encoding="utf-8"?>
<resources>
    <!--
      Splash theme. The window background is the website's cream with the OLK logo, so
      the very first frame is already OLK rather than a white flash (the logo's inside is
      transparent and its lettering dark, so it needs the light background), and
      postSplashScreenTheme hands off to a bare Material theme once Compose takes over.
    -->
    <style name="Theme.Olk.Splash" parent="Theme.SplashScreen">
        <item name="windowSplashScreenBackground">@color/ic_launcher_background</item>
        <item name="windowSplashScreenAnimatedIcon">@mipmap/ic_launcher_foreground</item>
        <item name="postSplashScreenTheme">@style/Theme.Olk</item>
    </style>

    <style name="Theme.Olk" parent="android:Theme.Material.NoActionBar">
        <item name="android:windowBackground">@color/brand_slate</item>
    </style>
</resources>
